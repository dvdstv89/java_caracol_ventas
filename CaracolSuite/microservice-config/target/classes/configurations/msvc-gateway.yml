server:
  port: 8088

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enable: true
      routes:
        - id: tienda
          uri: lb://caracol-msvc-tienda
          predicates:
            - Path=/api/v1/tienda/**
        - id: mistral
          uri: lb://caracol-msvc-mistral
          predicates:
            - Path=/api/v1/mistral/**
        - id: zunpr
          uri: lb://caracol-msvc-zunpr
          predicates:
            - Path=/api/v1/zunpr/**
        - id: organizacion-empresarial
          uri: lb://caracol-msvc-organizacion-empresarial
          predicates:
            - Path=/api/v1/centro-gestion/**