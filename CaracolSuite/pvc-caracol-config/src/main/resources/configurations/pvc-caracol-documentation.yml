server:
  port: 9010

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

api-docs:
  enable: true
swagger-ui:
   enable: true
#  packagesToScan: pvc.caracol.empresarial.controller